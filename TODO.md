# TODO List

- tests for new IArray methods
- tests for new IMap methods
- add section `<Func/Predicate>#Negation`
- add SubList view?
- reversed list view?
- add `GenericSet`
- test `IMap#TryDelete()` and `IMap#TryGet()` methods

- add mapper functions to access an object property by name, for example `Prop(Name) => (Obj) => Obj.%Name%`